// import { fromJS } from 'immutable';
// import { makeSelectSelectionPageDomain } from '../selectors';

// const selector = makeSelectSelectionPageDomain();

describe('makeSelectSelectionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
